<Window x:Class="THUAO.WarningWindow" 
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:THUAO"
        mc:Ignorable="d"
        Title="Cảnh báo" Height="150" Width="300"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        WindowStartupLocation="CenterScreen">

    <Window.Resources>
        <Storyboard x:Key="FadeIn">
            <DoubleAnimation Storyboard.TargetProperty="Opacity" From="0" To="1" Duration="0:0:0.3"/>
        </Storyboard>
        <Storyboard x:Key="FadeOut">
            <DoubleAnimation Storyboard.TargetProperty="Opacity" From="1" To="0" Duration="0:0:0.3"/>
        </Storyboard>
    </Window.Resources>

    <Border x:Name="WarningBorder" Margin="0,-6,0,0">
        <Border.Background>
            <ImageBrush ImageSource="Assets/button/warning/bg.png" Stretch="Fill"/>
        </Border.Background>

        <Grid Margin="10">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <!-- Tiêu đề -->
                <RowDefinition Height="*"/>
                <!-- Nội dung cảnh báo -->
                <RowDefinition Height="Auto"/>
                <!-- Nút đóng -->
            </Grid.RowDefinitions>

            <!-- Tiêu đề nằm giữa khung -->
            <TextBlock Grid.Row="0"
                       Text="Cảnh báo"
                       FontSize="20" FontWeight="Bold" Foreground="#D32F2F"
                       HorizontalAlignment="Center" VerticalAlignment="Center" Margin="0,5"/>

            <!-- Nội dung cảnh báo -->
            <Grid Grid.Row="1" Margin="10">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <Image x:Name="WarningIcon" Width="30" Height="30" Margin="0,0,10,0" Visibility="Collapsed"/>
                <TextBlock x:Name="MessageTextBlock" Grid.Column="1" TextWrapping="Wrap" FontSize="15"
                   VerticalAlignment="Center" Foreground="White"
                   Text="Không đủ xu để thực hiện hành động này!"/>
            </Grid>

            <!-- Nút đóng hình ảnh nằm giữa, sát viền dưới -->
            <Button Grid.Row="2"
                    x:Name="CloseButton" Width="30" Height="30" 
                    HorizontalAlignment="Center" VerticalAlignment="Bottom" Margin="0,0,0,5"
                    Background="Transparent" BorderThickness="0"
                    Click="CloseButton_Click"
                    Cursor="Hand">
                <Image Source="Assets/button/warning/close.png" Stretch="Uniform"/>
            </Button>
        </Grid>
    </Border>
</Window>
